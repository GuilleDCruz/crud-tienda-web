export interface SucursalResponse {
    id: number;
    nombre: string;
    codigo: string;
    direccion: string;
    telefono: string;
    email: string;
    activo: boolean;

    createdAt: string;
    updatedAt: string;
}